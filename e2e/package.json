{
  "name": "e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "pnpm exec playwright install --with-deps chromium",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
